<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.androidproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name=".StoreAcitvity"></activity>
        <activity android:name=".StoreListActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".PasswordConfirmActivity" />
        <activity android:name=".MasterActivity" />
        <activity android:name=".OrderActivity" />
        <activity
            android:name=".TableInformationActivity"
            android:noHistory="true" />
        <activity android:name=".TableOrderActivity" />
        <activity android:name=".CreateUserActivity" /> <!-- noHistory 효과 필요함 -->
        <activity android:name=".informationActivity" />
        <activity android:name=".FindIdActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>